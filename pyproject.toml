[project]
name = "curahack-2026-challenge-5-main"
version = "0.1.5"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kevin D Goveas", email = "kevdanngovead@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "biom-format>=2.1.17",
    "catboost>=1.2.10",
    "numpy>=1.26.4",
    "pandas>=2.1.4",
    "pycaret>=3.3.2",
]

[project.scripts]
gai-cal = "curahack_2026_challenge_5_main:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pandas-stubs>=3.0.5.260730",
]
